# macOS
.DS_Store

# Claude Code local settings
.claude/settings.local.json
.claude/*.tmp.*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# Model artifacts / large outputs
*.pth
*.pt
*.ckpt
checkpoints/
runs/
wandb/

# ...but DO keep our trained checkpoints: they are ~120 KB each and having them
# in the repo means a fresh clone can reproduce every figure without retraining.
!results/model_*.pth

# Cloned upstream repos (see README)
external/
